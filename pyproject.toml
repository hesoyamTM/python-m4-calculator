[project]
name = "python-m4-calculator"
version = "0.1.0"
description = "Calculator with custom variables and functions for MAI lab"
requires-python = ">=3.12"
dependencies = ["pytest>=8.4.2", "pytest-mypy>=1.0.1"]
